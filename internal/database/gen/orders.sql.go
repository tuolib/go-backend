// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: orders.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createOrder = `-- name: CreateOrder :one
INSERT INTO order_service.orders (id, order_no, user_id, status, total_amount, discount_amount, pay_amount, remark, idempotency_key, expires_at, created_at, updated_at)
VALUES ($1, $2, $3, 'pending', $4, $5, $6, $7, $8, $9, NOW(), NOW())
RETURNING id, order_no, user_id, status, total_amount, discount_amount, pay_amount, payment_method, payment_no, paid_at, shipped_at, delivered_at, completed_at, cancelled_at, cancel_reason, remark, idempotency_key, expires_at, version, created_at, updated_at
`

type CreateOrderParams struct {
	ID             string             `json:"id"`
	OrderNo        string             `json:"order_no"`
	UserID         string             `json:"user_id"`
	TotalAmount    pgtype.Numeric     `json:"total_amount"`
	DiscountAmount pgtype.Numeric     `json:"discount_amount"`
	PayAmount      pgtype.Numeric     `json:"pay_amount"`
	Remark         pgtype.Text        `json:"remark"`
	IdempotencyKey pgtype.Text        `json:"idempotency_key"`
	ExpiresAt      pgtype.Timestamptz `json:"expires_at"`
}

// 创建订单。
// Create an order.
func (q *Queries) CreateOrder(ctx context.Context, arg CreateOrderParams) (OrderServiceOrder, error) {
	row := q.db.QueryRow(ctx, createOrder,
		arg.ID,
		arg.OrderNo,
		arg.UserID,
		arg.TotalAmount,
		arg.DiscountAmount,
		arg.PayAmount,
		arg.Remark,
		arg.IdempotencyKey,
		arg.ExpiresAt,
	)
	var i OrderServiceOrder
	err := row.Scan(
		&i.ID,
		&i.OrderNo,
		&i.UserID,
		&i.Status,
		&i.TotalAmount,
		&i.DiscountAmount,
		&i.PayAmount,
		&i.PaymentMethod,
		&i.PaymentNo,
		&i.PaidAt,
		&i.ShippedAt,
		&i.DeliveredAt,
		&i.CompletedAt,
		&i.CancelledAt,
		&i.CancelReason,
		&i.Remark,
		&i.IdempotencyKey,
		&i.ExpiresAt,
		&i.Version,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getOrderByID = `-- name: GetOrderByID :one

SELECT id, order_no, user_id, status, total_amount, discount_amount, pay_amount, payment_method, payment_no, paid_at, shipped_at, delivered_at, completed_at, cancelled_at, cancel_reason, remark, idempotency_key, expires_at, version, created_at, updated_at FROM order_service.orders
WHERE id = $1
`

// 订单查询
// Order queries
// 按 ID 查订单。
// Get order by ID.
func (q *Queries) GetOrderByID(ctx context.Context, id string) (OrderServiceOrder, error) {
	row := q.db.QueryRow(ctx, getOrderByID, id)
	var i OrderServiceOrder
	err := row.Scan(
		&i.ID,
		&i.OrderNo,
		&i.UserID,
		&i.Status,
		&i.TotalAmount,
		&i.DiscountAmount,
		&i.PayAmount,
		&i.PaymentMethod,
		&i.PaymentNo,
		&i.PaidAt,
		&i.ShippedAt,
		&i.DeliveredAt,
		&i.CompletedAt,
		&i.CancelledAt,
		&i.CancelReason,
		&i.Remark,
		&i.IdempotencyKey,
		&i.ExpiresAt,
		&i.Version,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getOrderByIDAndUser = `-- name: GetOrderByIDAndUser :one
SELECT id, order_no, user_id, status, total_amount, discount_amount, pay_amount, payment_method, payment_no, paid_at, shipped_at, delivered_at, completed_at, cancelled_at, cancel_reason, remark, idempotency_key, expires_at, version, created_at, updated_at FROM order_service.orders
WHERE id = $1 AND user_id = $2
`

type GetOrderByIDAndUserParams struct {
	ID     string `json:"id"`
	UserID string `json:"user_id"`
}

// 按 ID + 用户查订单（防止越权查看他人订单）。
// Get order by ID + user (prevents unauthorized access to others' orders).
func (q *Queries) GetOrderByIDAndUser(ctx context.Context, arg GetOrderByIDAndUserParams) (OrderServiceOrder, error) {
	row := q.db.QueryRow(ctx, getOrderByIDAndUser, arg.ID, arg.UserID)
	var i OrderServiceOrder
	err := row.Scan(
		&i.ID,
		&i.OrderNo,
		&i.UserID,
		&i.Status,
		&i.TotalAmount,
		&i.DiscountAmount,
		&i.PayAmount,
		&i.PaymentMethod,
		&i.PaymentNo,
		&i.PaidAt,
		&i.ShippedAt,
		&i.DeliveredAt,
		&i.CompletedAt,
		&i.CancelledAt,
		&i.CancelReason,
		&i.Remark,
		&i.IdempotencyKey,
		&i.ExpiresAt,
		&i.Version,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getOrderByIdempotencyKey = `-- name: GetOrderByIdempotencyKey :one
SELECT id, order_no, user_id, status, total_amount, discount_amount, pay_amount, payment_method, payment_no, paid_at, shipped_at, delivered_at, completed_at, cancelled_at, cancel_reason, remark, idempotency_key, expires_at, version, created_at, updated_at FROM order_service.orders
WHERE idempotency_key = $1
`

// 按幂等键查订单（创建订单时去重）。
// Get order by idempotency key (dedup on order creation).
func (q *Queries) GetOrderByIdempotencyKey(ctx context.Context, idempotencyKey pgtype.Text) (OrderServiceOrder, error) {
	row := q.db.QueryRow(ctx, getOrderByIdempotencyKey, idempotencyKey)
	var i OrderServiceOrder
	err := row.Scan(
		&i.ID,
		&i.OrderNo,
		&i.UserID,
		&i.Status,
		&i.TotalAmount,
		&i.DiscountAmount,
		&i.PayAmount,
		&i.PaymentMethod,
		&i.PaymentNo,
		&i.PaidAt,
		&i.ShippedAt,
		&i.DeliveredAt,
		&i.CompletedAt,
		&i.CancelledAt,
		&i.CancelReason,
		&i.Remark,
		&i.IdempotencyKey,
		&i.ExpiresAt,
		&i.Version,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listExpiredPendingOrders = `-- name: ListExpiredPendingOrders :many
SELECT id, order_no, user_id, status, total_amount, discount_amount, pay_amount, payment_method, payment_no, paid_at, shipped_at, delivered_at, completed_at, cancelled_at, cancel_reason, remark, idempotency_key, expires_at, version, created_at, updated_at FROM order_service.orders
WHERE status = 'pending' AND expires_at < NOW()
ORDER BY expires_at ASC
LIMIT 100
`

// 查找已超时的待支付订单（定时取消任务用，每次最多 100 条）。
// Find expired pending orders (for auto-cancellation job, max 100 per batch).
func (q *Queries) ListExpiredPendingOrders(ctx context.Context) ([]OrderServiceOrder, error) {
	rows, err := q.db.Query(ctx, listExpiredPendingOrders)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []OrderServiceOrder{}
	for rows.Next() {
		var i OrderServiceOrder
		if err := rows.Scan(
			&i.ID,
			&i.OrderNo,
			&i.UserID,
			&i.Status,
			&i.TotalAmount,
			&i.DiscountAmount,
			&i.PayAmount,
			&i.PaymentMethod,
			&i.PaymentNo,
			&i.PaidAt,
			&i.ShippedAt,
			&i.DeliveredAt,
			&i.CompletedAt,
			&i.CancelledAt,
			&i.CancelReason,
			&i.Remark,
			&i.IdempotencyKey,
			&i.ExpiresAt,
			&i.Version,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateOrderCancelled = `-- name: UpdateOrderCancelled :exec
UPDATE order_service.orders
SET status = 'cancelled',
    cancelled_at = NOW(),
    cancel_reason = $2,
    version = version + 1,
    updated_at = NOW()
WHERE id = $1 AND version = $3
`

type UpdateOrderCancelledParams struct {
	ID           string      `json:"id"`
	CancelReason pgtype.Text `json:"cancel_reason"`
	Version      int32       `json:"version"`
}

// 取消订单。
// Cancel an order.
func (q *Queries) UpdateOrderCancelled(ctx context.Context, arg UpdateOrderCancelledParams) error {
	_, err := q.db.Exec(ctx, updateOrderCancelled, arg.ID, arg.CancelReason, arg.Version)
	return err
}

const updateOrderPaid = `-- name: UpdateOrderPaid :exec
UPDATE order_service.orders
SET status = 'paid',
    payment_method = $2,
    payment_no = $3,
    paid_at = NOW(),
    version = version + 1,
    updated_at = NOW()
WHERE id = $1 AND status = 'pending' AND version = $4
`

type UpdateOrderPaidParams struct {
	ID            string      `json:"id"`
	PaymentMethod pgtype.Text `json:"payment_method"`
	PaymentNo     pgtype.Text `json:"payment_no"`
	Version       int32       `json:"version"`
}

// 标记订单为已支付。
// Mark order as paid.
func (q *Queries) UpdateOrderPaid(ctx context.Context, arg UpdateOrderPaidParams) error {
	_, err := q.db.Exec(ctx, updateOrderPaid,
		arg.ID,
		arg.PaymentMethod,
		arg.PaymentNo,
		arg.Version,
	)
	return err
}

const updateOrderStatus = `-- name: UpdateOrderStatus :exec
UPDATE order_service.orders
SET status = $2,
    version = version + 1,
    updated_at = NOW()
WHERE id = $1 AND version = $3
`

type UpdateOrderStatusParams struct {
	ID      string `json:"id"`
	Status  string `json:"status"`
	Version int32  `json:"version"`
}

// 更新订单状态（使用乐观锁）。
// Update order status (with optimistic locking).
func (q *Queries) UpdateOrderStatus(ctx context.Context, arg UpdateOrderStatusParams) error {
	_, err := q.db.Exec(ctx, updateOrderStatus, arg.ID, arg.Status, arg.Version)
	return err
}
