// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: categories.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createCategory = `-- name: CreateCategory :one
INSERT INTO product_service.categories (id, parent_id, name, slug, icon_url, sort_order, is_active, created_at, updated_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, NOW(), NOW())
RETURNING id, parent_id, name, slug, icon_url, sort_order, is_active, created_at, updated_at
`

type CreateCategoryParams struct {
	ID        string      `json:"id"`
	ParentID  pgtype.Text `json:"parent_id"`
	Name      string      `json:"name"`
	Slug      string      `json:"slug"`
	IconUrl   pgtype.Text `json:"icon_url"`
	SortOrder int32       `json:"sort_order"`
	IsActive  bool        `json:"is_active"`
}

// 创建分类。
// Create a category.
func (q *Queries) CreateCategory(ctx context.Context, arg CreateCategoryParams) (ProductServiceCategory, error) {
	row := q.db.QueryRow(ctx, createCategory,
		arg.ID,
		arg.ParentID,
		arg.Name,
		arg.Slug,
		arg.IconUrl,
		arg.SortOrder,
		arg.IsActive,
	)
	var i ProductServiceCategory
	err := row.Scan(
		&i.ID,
		&i.ParentID,
		&i.Name,
		&i.Slug,
		&i.IconUrl,
		&i.SortOrder,
		&i.IsActive,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteCategory = `-- name: DeleteCategory :exec
DELETE FROM product_service.categories WHERE id = $1
`

// 删除分类（物理删除）。
// Delete a category (hard delete).
func (q *Queries) DeleteCategory(ctx context.Context, id string) error {
	_, err := q.db.Exec(ctx, deleteCategory, id)
	return err
}

const getCategoryByID = `-- name: GetCategoryByID :one
SELECT id, parent_id, name, slug, icon_url, sort_order, is_active, created_at, updated_at FROM product_service.categories
WHERE id = $1
`

// 按 ID 查分类。
// Get category by ID.
func (q *Queries) GetCategoryByID(ctx context.Context, id string) (ProductServiceCategory, error) {
	row := q.db.QueryRow(ctx, getCategoryByID, id)
	var i ProductServiceCategory
	err := row.Scan(
		&i.ID,
		&i.ParentID,
		&i.Name,
		&i.Slug,
		&i.IconUrl,
		&i.SortOrder,
		&i.IsActive,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getCategoryBySlug = `-- name: GetCategoryBySlug :one
SELECT id, parent_id, name, slug, icon_url, sort_order, is_active, created_at, updated_at FROM product_service.categories
WHERE slug = $1
`

// 按 slug 查分类。
// Get category by slug.
func (q *Queries) GetCategoryBySlug(ctx context.Context, slug string) (ProductServiceCategory, error) {
	row := q.db.QueryRow(ctx, getCategoryBySlug, slug)
	var i ProductServiceCategory
	err := row.Scan(
		&i.ID,
		&i.ParentID,
		&i.Name,
		&i.Slug,
		&i.IconUrl,
		&i.SortOrder,
		&i.IsActive,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listActiveCategories = `-- name: ListActiveCategories :many
SELECT id, parent_id, name, slug, icon_url, sort_order, is_active, created_at, updated_at FROM product_service.categories
WHERE is_active = true
ORDER BY sort_order ASC, name ASC
`

// 获取所有启用的分类。
// Get all active categories.
func (q *Queries) ListActiveCategories(ctx context.Context) ([]ProductServiceCategory, error) {
	rows, err := q.db.Query(ctx, listActiveCategories)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProductServiceCategory{}
	for rows.Next() {
		var i ProductServiceCategory
		if err := rows.Scan(
			&i.ID,
			&i.ParentID,
			&i.Name,
			&i.Slug,
			&i.IconUrl,
			&i.SortOrder,
			&i.IsActive,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listCategories = `-- name: ListCategories :many

SELECT id, parent_id, name, slug, icon_url, sort_order, is_active, created_at, updated_at FROM product_service.categories
ORDER BY sort_order ASC, name ASC
`

// 分类查询
// Category queries
// 获取所有分类（扁平列表），按 sort_order 排序。
// Get all categories (flat list), sorted by sort_order.
func (q *Queries) ListCategories(ctx context.Context) ([]ProductServiceCategory, error) {
	rows, err := q.db.Query(ctx, listCategories)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProductServiceCategory{}
	for rows.Next() {
		var i ProductServiceCategory
		if err := rows.Scan(
			&i.ID,
			&i.ParentID,
			&i.Name,
			&i.Slug,
			&i.IconUrl,
			&i.SortOrder,
			&i.IsActive,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateCategory = `-- name: UpdateCategory :one
UPDATE product_service.categories
SET name = COALESCE($2, name),
    slug = COALESCE($3, slug),
    parent_id = $4,
    icon_url = COALESCE($5, icon_url),
    sort_order = COALESCE($6, sort_order),
    is_active = COALESCE($7, is_active),
    updated_at = NOW()
WHERE id = $1
RETURNING id, parent_id, name, slug, icon_url, sort_order, is_active, created_at, updated_at
`

type UpdateCategoryParams struct {
	ID        string      `json:"id"`
	Name      string      `json:"name"`
	Slug      string      `json:"slug"`
	ParentID  pgtype.Text `json:"parent_id"`
	IconUrl   pgtype.Text `json:"icon_url"`
	SortOrder int32       `json:"sort_order"`
	IsActive  bool        `json:"is_active"`
}

// 更新分类。
// Update a category.
func (q *Queries) UpdateCategory(ctx context.Context, arg UpdateCategoryParams) (ProductServiceCategory, error) {
	row := q.db.QueryRow(ctx, updateCategory,
		arg.ID,
		arg.Name,
		arg.Slug,
		arg.ParentID,
		arg.IconUrl,
		arg.SortOrder,
		arg.IsActive,
	)
	var i ProductServiceCategory
	err := row.Scan(
		&i.ID,
		&i.ParentID,
		&i.Name,
		&i.Slug,
		&i.IconUrl,
		&i.SortOrder,
		&i.IsActive,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
